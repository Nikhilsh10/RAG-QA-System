# requirements-test.txt
# Lightweight dependencies for running the test suite in CI.
# Heavy ML libraries (torch, sentence-transformers, faiss-cpu) are mocked
# in the tests so they do NOT need to be installed.

fastapi==0.111.0
httpx>=0.27.0          # required by fastapi TestClient
pytest>=8.0
pytest-cov>=5.0
pydantic>=2.7.4
python-dotenv==1.0.1
requests==2.32.2
langchain==0.2.1
langchain-community==0.2.1
langchain-huggingface==0.0.3
pypdf==4.2.0
python-multipart==0.0.9
docx2txt==0.8
